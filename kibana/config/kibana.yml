# Kibana配置文件

# 服务器配置
server.host: "0.0.0.0"
server.port: 5601
server.name: "NewMind AI Platform"

# Elasticsearch连接（无认证）
elasticsearch.hosts: ["http://es01:9200"]

# 监控配置
monitoring.ui.enabled: true

# 注意：自定义品牌功能(xpackCustomBranding)在Kibana 8.17.3中不可用
# 需要企业版许可证或更高版本才支持
